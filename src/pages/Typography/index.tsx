import { AnimateText } from "src/components";
import styles from "src/styles/Typography/styles.module.css";

const Typography = () => {
  return (
    <div className={styles.container}>
      <AnimateText />
    </div>
  );
};

export default Typography;
