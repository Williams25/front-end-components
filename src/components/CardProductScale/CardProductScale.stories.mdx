import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { CardProductScale } from "./index";

<Meta title="Components/Cards/CardProductScale" component={CardProductScale} />

# CardProductScale

<Canvas>
  <Story name="CardProductScale">
    {(args) => (
      <div
        style={{ display: "flex", justifyContent: "center", marginTop: "3rem" }}
      >
        <CardProductScale {...args} />
      </div>
    )}
  </Story>
</Canvas>

# TSX

```tsx

```

# CSS

```css

```
